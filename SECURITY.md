# 安全说明

- 私密简历、联系方式、微信二维码和附件简历均以 AES-GCM 加密后发布。
- 密钥由访问密码通过 PBKDF2-SHA-256 派生，密码不会写入仓库。
- `private-resume.json` 是本地明文源文件，已通过 `.gitignore` 排除，不应提交。
- GitHub Pages 使用 HTTPS，适合部署这个静态加密页面。
- 当前密码为临时发布密码，正式长期使用前建议更换为更长、更随机的密码，并重新加密私密数据和附件。
